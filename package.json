{
  "name": "nutri-react",
  "version": "1.0.0",
  "type": "module",
  "description": "Nutriscore by valouleloup",
  "scripts": {
    "start": "npx babel --watch src --out-dir generated --presets react-app/prod",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "valouleloup",
  "license": "ISC",
  "dependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-react-app": "^3.1.2"
  }
}
